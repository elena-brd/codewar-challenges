function greet(name) {
    return `Hello, ${name} how are you doing today? `;
}

const greet2 = (name) => `Hello, ${name} how are you doing today? `;

console.log(greet('Jane'));
console.log(greet2('Joe'));